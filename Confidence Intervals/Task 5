# Генерирайте данни x1,x2,..,xn от равномерно разпределение в интервала
# (5 9). Проверете хипотезата mu= 7 срещу mu != 7. Повторете 10000 пъти за n = 20,50,100,500.
# В каква част от случаите нулевата хипотеза не се отхвърля?


gen2<-function(n,alpha)
{
  x<-runif(n,5,9)
  p.val <- t.test(x, mu=7)$p.value
  cnd2 <- ( p.val > alpha )
  cnd2
}

res2 <- replicate( 10000, gen2(n=500, alpha=0.05) )
table(res2)/length(res2)
