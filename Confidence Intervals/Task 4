# Генерирайте данни x1,x2,...,xn от равномерно разпределение в интервала
# (5 9). Намерете 95-процентен доверителен интервал за средното . Повторете 10000 пъти
# за n =20,50,100,500. В каква част от случаите доверителният интервал съдържа 7?


gen<-function(n,alpha)
{
  x<-runif(n,5,9)
  ci <- t.test(x, conf.level=1-alpha)$conf.int[1:2]
  cnd1 <- ( ci[1] <= 7 & ci[2] >= 7 )
  cnd1
}
res1 <- replicate( 10000, gen(n=500, alpha=0.05) )
table(res1)/length(res1)
