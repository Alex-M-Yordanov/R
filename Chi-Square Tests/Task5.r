# Симулирайте n хвърляния на балансиран зар. Като използвате генерираните
# данни, проверете хипотезата, че всяка от страните се пада с една и съща вероятност.
# Повторете 10000 пъти за n = 100,200,400. Колко често заключението на теста е вярно?

dice<-function(n)
{
  dice<-sample(c(1:6),10,replace=T)
  x <- tabulate(dice, nbins=6)
  chisq.test( x, p=rep(1/6, 6) )$p.value
}

res <- replicate( 10000, dice(100) )
sum(res>0.05)/length(res)
